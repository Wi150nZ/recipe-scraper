import { expect } from 'chai';
import { SpendWithPennies } from './spend-with-pennies';

describe('SpendWithPennies middleware', () => {
  it('passes to the next middleware if the url does not match', () => {

  });
});